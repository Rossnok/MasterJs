//bucle for es una estructura de control que se repite varias veces, puede ser definido o indefinido, normalmente definido

//Bucle for
//Esta formado por la siguiente estructura (contador; condicion; incrementador en cada iteracion)
for(var i = 0; i <= 100; i++){
    console.log(`estamos en el numero ${i}`);

   // debugger;// esta es una funcion que permite observar las iteraciones en la consola del navegador, permitiendo asi observar cada iteracion.
}